inference:

  checkpoint: "D:/Project/Model/Weights/AIRace/iter_5000_fixed.pth"

  output:
    directory: "./outputs/bts_scene_01/novel_views"   # đây là THƯ MỤC lưu ảnh render, không phải đường dẫn checkpoint

    zip: false
    zip_name: "submission.zip"

  render:

    images: true

    video:
      enabled: true
      fps: 24
      codec: "mp4"